import React from "react";
import ReactDOM from "react-dom";
import EntireComponent from "./components/EntireComponent";
import "./stylesheets/css/main.css";

ReactDOM.render(
  <React.StrictMode>
    <EntireComponent></EntireComponent>
  </React.StrictMode>,
  document.getElementById("root")
);
